# لغة التصميم — منصة علامة لحوكمة المشاريع

هذا المرجع **ليس نظريًا**. كل نمط فيه بُني وطُبِّق واختُبر في المنصة، وأغلبه نشأ من إصلاح مشكلة حقيقية وقعت فعلًا. الغرض منه أن تتبع أي إضافة قادمة الأنماط الناضجة بدل إعادة اختراعها — أو تكرار أخطاء عولجت.

---

## ١. الإجراء الأساسي الواحد

**القاعدة:** في أي لحظة، للكيان **إجراء واحد صحيح**. اعرضه بارزًا، وضع ما عداه في قائمة `⋯`.

**سبب النشأة:** لوحة العقد كانت تعرض تسعة أزرار متساوية الوزن في الرأس. المستخدم يرى كل شيء دائمًا، فلا يرى الشيء المهم.

```js
const STAGE = {primary:null, secondary:[], notes:[], tone:'', title:'', hint:''};
if (!approved)      STAGE.primary = {id:'chdApprove', label:'✅ اعتماد داخلي', color:'var(--ok)'};
else if (!signedBy) STAGE.primary = {id:'chdSignNow', label:'✍️ توقيع علامة', color:'var(--ok)'};
```

**شروط لازمة:**
- الإجراء الأساسي **لا يتكرر** في القائمة الثانوية.
- القائمة `⋯` **تُخفى كليًا** إن كانت فارغة — لا زر ميت.
- تُغلق بالنقر خارجها وبعد اختيار إجراء، وتحمل `aria-haspopup` و`aria-expanded`.

---

## ٢. شريط المرحلة الواحد

**القاعدة:** شريط واحد يجيب على سؤالين: **أين نحن؟** و**ما الخطوة التالية؟** — لا شرائط متعددة متنافسة.

**سبب النشأة:** كانت خمسة أنماط شرائط تظهر متتالية فوق بعضها، بلغت حدّ عرض **نفس الرسالة مرتين** حرفيًا من موضعين مختلفين.

```html
<div class="chub-stage {tone}">
  <div class="chub-stage-main">
    <b>بانتظار توقيع علامة</b>
    <span class="sa-hint">وقّع بصفتك ممثل علامة، ثم أرسل الرابط للشريك.</span>
  </div>
  <div class="chub-stage-notes"><!-- ملاحظات ثانوية كشارات صغيرة --></div>
</div>
```

**النبرات:** `wait` (منتظر) · `go` (جارٍ) · `done` (مكتمل) · `info` (سياق) · `void` (ملغى) · `muted` (مؤرشف).

**الملاحظات الثانوية تُدمج في الشريط** كشارات — لا تُصنع لها شرائط مستقلة.

---

## ٣. صف القائمة يجيب ثلاثة أسئلة

**القاعدة:** أي عقد/مشروع؟ · مع من؟ · ما الخطوة التالية؟

**سبب النشأة:** صف العقد كان يحشر حتى **سبع شارات** في سطر ويعرض الحالة الخام.

```
● C-2026-1005  عقد سنام   [نص مخصَّص]        يحتاج اعتمادك
  سنام · غير مرتبط بمشروع · 10 ر.س
```

**قواعد:**
- **الصياغة موجَّهة للمستخدم:** «يحتاج اعتمادك» لا `pending_alamaa`.
- نقطة لونية للمسح السريع + شارة الخطوة التالية.
- **الشارات تقتصر على ما يغيّر القرار** — لا كل ما يمكن عرضه.
- دالة مشتركة (`rowStage`) تضمن **ألا تتناقض القائمة واللوحة** في وصف نفس الحالة.

---

## ٤. الحالة الفارغة توجّه ولا تُخبر

**القاعدة:** كل حالة فارغة تجيب: **لماذا هي فارغة؟** و**ما الخطوة؟** — وزر حين يوجد فعل ممكن.

**سبب النشأة:** كانت كلها نصوصًا جافة («لا تنبيهات.») تترك المستخدم لا يعرف إن كان الفراغ طبيعيًا أم خللًا.

```js
emptyState({
  icon:'👍', title:'لا تنبيهات',
  hint:'لا تأخير ولا بند متوقف ولا متطلب متأخر — المشروع على المسار.'
})
```

**تمييز إلزامي:** «لا نتائج لهذا الفلتر» (بزر مسح) ≠ «لا شيء بعد إطلاقًا» (بدعوة للإنشاء).

**تفصيل تقني:** المكوّن يقبل `wrap` لأن نصف الحالات داخل `<ul>` — وإدراج `<div>` هناك يكسر البنية. والنص **يُهرَّب** فلا يُحقن HTML.

---

## ٥. التبويبات تفصل القراءة عن التحرير

**القاعدة:** لا تخلط العرض بالتحرير في تدفّق واحد، ورتّب بمنطق **الاستخدام** لا البناء.

```
نظرة عامة │ الشروط والبنود │ الملاحق │ الإرسال والتوقيع │ السجل
```

- **التبويب الافتراضي يتبع الحالة** لا ترتيبًا ثابتًا: غير معتمد ← الشروط · بانتظار التوقيع ← الإرسال.
- **التبديل بلا إعادة بناء** — يحفظ ما كُتب في الحقول.
- **أي حفظ يُعيد لنفس التبويب** لا يقفز للبداية.
- المرجع نادر الاستخدام (السجل) في الآخر، وأداة التحقق الأهم (المعاينة) في الأول.

---

## ٦. القوائم الطويلة تُجمَّع

**القاعدة:** أكثر من ٧–٨ بنود ← مجموعات معنونة بفواصل و`role="group"`.

**مثال مطبَّق:** أدوات المكتب (١٤ بندًا) → عروض شاملة · إدارة · إعدادات.

---

## ٧. رسائل الخطأ تشرح وتقترح

**القاعدة:** لا ترفض بلا سبب ولا طريق.

| ✗ | ✓ |
|---|---|
| «تعذّر التعديل» | «لا يمكن تعديل عقد وقّع عليه أي طرف — أنشئ ملحق تعديل بدلًا من ذلك» |
| «صلاحية غير كافية» | «الاعتماد الداخلي لمالك/مدير المنصة حصرًا» |
| «خطأ» | «الخطة مثبَّتة — التعديل البنيوي يحتاج طلب تغيير معتمَدًا أولًا» |

---

## ٨. الإجراء الخطر يُفسَّر قبل تنفيذه

**القاعدة:** الحوار يذكر **ما سيحدث فعلًا** وما **لن** يحدث.

- حلّ حزمة: «ستُحذف الحزمة وتبقى بنودها الـ٥ كما هي — **لن يُحذف أي بند**».
- تفعيل الأتمتة: «ستُرسَل رسائل لشركائك **دون تدخل منك في كل مرة**».
- التصدير: يعدّد محتويات المستند **قبل** فتح حوار الطباعة.

---

## ٩. الأتمتة معطَّلة افتراضيًا

**القاعدة:** ما يُرسِل رسائل للشركاء أو يعدّل بيانات بلا تدخل — يبدأ **مغلقًا** ولا يُفتح إلا بقرار صريح مع تأكيد. ومعه سقف يمنع الإفراط، ومعاينة لما سيشمله **قبل** التفعيل.

---

## ١٠. إتاحة الوصول ليست اختيارية

- كل زر أيقوني: `aria-label`.
- الرموز الزخرفية: `aria-hidden="true"`.
- صفوف القوائم القابلة للنقر: `role="button"` + `tabindex="0"` + وصف يجمع الاسم والحالة.
- التبويبات: `role="tablist"` · `aria-selected` · تنقّل بالأسهم **يراعي RTL** · `Home/End`.

---

## ١١. الطباعة تُستعاد بثلاثة مسارات

**سبب النشأة:** الاستعادة كانت معلَّقة على `afterprint` وحده — فإن لم يُطلقه المتصفح يبقى التطبيق عالقًا في وضع الطباعة (أو الجانت مصغَّرًا) ويبدو معطّلًا.

```js
window.addEventListener('afterprint', restore);   // المسار الطبيعي
window.addEventListener('focus', restore);        // إن أُغلق الحوار بلا حدث
const guard = setTimeout(restore, 60000);         // الملاذ الأخير
```

مع حارس `done` يمنع الاستعادة المزدوجة.

---

## ١٢. مصدر حقيقة واحد

**القاعدة:** لا تحسب نفس القيمة بطريقتين.

**مثالان مطبَّقان:**
- **حِمل العمل** يستدعي `scheduleTasks` نفسها المستخدَمة في الجانت. لو حُسبت الجدولة في SQL لأنتجت أرقامًا تخالف ما يراه المستخدم — وهذا أسوأ من غياب الميزة.
- **`rowStage`** مشتركة بين القائمة واللوحة فلا تتناقضان.

---

## ١٣. الحماية في أدنى طبقة

**القاعدة:** الحارس في القاعدة لا في الدالة وحدها.

**سبب النشأة:** حواجز تعديل العقد الموقَّع كانت داخل الدالة فقط، بينما سياسة `UPDATE` تسمح بتعديل الصف مباشرة عبر PostgREST — استدعاء واحد كان يتجاوزها كليًا.

**قاعدة مرافقة:** `create or replace` مع **تغيير التوقيع يُنشئ دالة جديدة ولا يستبدل القديمة**. النسخة القديمة تبقى حيّة وقابلة للاستدعاء — وقد سبّب هذا فعلًا ثغرة تجاوز تحقق الهوية. **احذف التوقيع القديم صراحة**، وشغّل «🛡 فحص أمني» بعد كل جولة.

---

## ١٤. الاختبار يسبق الثقة

- **بوابة تحقق بعد كل خطوة** — لا اكتفاء برسالة «نجح».
- **اختبار تصييري** (بناء DOM فعلي) لا فحص نصوص فقط، فهو يمسك ما لا يمسكه الآخر.
- **تحقق حيّ على القاعدة** للضمانات الجوهرية (العزل، التجميد، الصلاحيات) ثم تنظيف بيانات الاختبار.
- عند فشل تأكيد بعد إعادة تصميم: **تأكد أن القدرة باقية وموضعها تغيّر** → حدّث التأكيد. لا تعطّله.

---

## ملاحظة منهجية أخيرة

**القياس الخام بلا تفكيك يُنتج استنتاجًا خاطئًا.** قست مرة كثافة شاشة المشروع بأمر واحد على مستوى الملف فخرجت بـ«٧٠ عنصرًا — تحتاج إعادة تصميم». القياس المفكَّك أظهر أن أكثف شاشة فيها ٢٠ عنصرًا وهي جدول بيانات، وأن التبويبات مبنية بمعايير سليمة أصلًا. **فأُلغيت إعادة التصميم** — تغيير منطق مستقر بلا مبرر مخاطرة لا إنجاز.
