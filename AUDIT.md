# تحليل هندسي — منصّة علامة لحوكمة المشاريع

**تاريخ التحليل:** 2026-08-16 · **الحالة المفحوصة:** `main` @ 50 دفعة · **الاختبارات وقت الفحص:** ✅ 468 نجاح · صفر فشل

هذا مستند **تشخيص**، لا قائمة أمنيات. كل فجوة أدناه مقترنة بدليل قابل لإعادة القياس (أمر أو رقم أو موضع في الملف)، ومصنَّفة بأثرها لا بسهولة إصلاحها. خارطة العلاج في «المسار الهندسي» ضمن [`ROADMAP.md`](ROADMAP.md).

---

## القسم صفر — ما هو ناضج فعلًا (لا يُمسّ)

الحكم على مشروع يبدأ بمعرفة ما يعمل. هذه المنصة **ليست كودًا فوضويًا**؛ فيها انضباط هندسي حقيقي، وكثير من «أفضل الممارسات» المعتادة مطبَّق هنا بالفعل وبدافع من حوادث وقعت لا من كتاب:

| الأصل الناضج | الدليل |
|---|---|
| محرك CPM خالص بلا تبعيات | `src/engine.js` — دوال نقيّة (`scheduleTasks`, `computeTracking`)، قابلة للاستدعاء من الاختبار مباشرة |
| عزل طبقة البيانات فعليًا لا شعارًا | كل `sb.from/rpc/auth` داخل `src/api.js` حصرًا — تحقّقتُ منه ووجدته مطبَّقًا |
| شبكة اختبارات تصييرية حقيقية | 23 ملفًا · 468 تأكيدًا · تبني DOM فعليًا بـjsdom لا نصوصًا |
| حارس نشر في CI | يمنع تكرار حادثة «نُشر المصدر ونُسي المُولَّد» — حارس نابع من ألم حقيقي |
| لغة تصميم مكتوبة ومُختبَرة | `DESIGN.md` + `test_design_language.js` + `test_empty_states_a11y.js` |
| حفظ التركيز وموضع التمرير عبر إعادة البناء | `preserveFocus` في `src/config.js:175` |
| استعادة الطباعة بثلاثة مسارات | `afterprint` + `focus` + مهلة حارسة — يعالج تعليق المتصفح |
| مصدر حقيقة واحد للجدولة | «حِمل العمل» يستدعي `scheduleTasks` نفسها بدل حسابها في SQL |

**الاستنتاج الأهم من هذا القسم:** الفجوات أدناه ليست فجوات «مشروع مبتدئ». هي فجوات **مشروع نجح فتجاوز حجمه بنيته**. هذا يغيّر العلاج جذريًا: العلاج **تطوير تدريجي محروس بالاختبارات**، لا إعادة كتابة.

---

## القسم أ — هيكلة الكود

### أ-١ · لا نظام وحدات — النطاق العام هو المعمارية 🔴 حرج

**القياس:** 403 دالة عامة + نحو 35 متغيّرًا عامًا قابلًا للتعديل، في نطاق واحد مشترك.

```bash
grep -rh "^\(async \)\?function [A-Za-z0-9_]*" src/*.js src/app/*.js | wc -l   # 403
sed -n '3p' src/app/state.js   # 18 متغيّرًا عامًا في سطر واحد
```

`build.py` يدمج 17 ملفًا **نصيًا** بلا `import`/`export`. لا حدود بين الوحدات، ولا شجرة تبعيات يمكن التحقق منها آليًا، وترتيب التحميل عقد شفهي مكتوب في تعليق.

**الأثر الحقيقي:** إعادة تسمية أي دالة تتطلب بحثًا نصيًا في المستودع كله، والتصادم — إن وقع — يُكتشف **وقت التشغيل عند المستخدم**، لا وقت البناء.

**إنصافًا:** فحصتُ التصادمات الفعلية اليوم فوجدتها **صفرًا**. الانضباط قائم — لكنه **بشري بالكامل وبلا ضمانة آلية**. هذا يصمد عند 403 دالة، ويسقط عند 600.

### أ-٢ · دوال عملاقة تخلط أربع مسؤوليات 🔴 حرج

| الدالة | الأسطر | الملف |
|---|---|---|
| `openContractDetailPanel` | **820** | `src/app/contractshub.js` (من 1274 سطرًا — **64% من الملف دالة واحدة**) |
| `renderContractsHubBody` | 169 | `src/app/contractshub.js` |
| `openNewContractPanel` | 133 | `src/app/contractshub.js` |
| `render` | 110 | `src/views.js` |

`openContractDetailPanel` تجمع في جسد واحد: **جلب بيانات** + **قواعد حالة** + **بناء HTML** + **ربط أحداث** + **استدعاءات شبكة**. لا يمكن اختبار قاعدة حالة واحدة فيها دون بناء اللوحة كاملة، ولا تغيير تبويب دون قراءة 820 سطرًا للتأكد أنك لم تكسر غيره.

**مفارقة تستحق الانتباه:** هذا الملف هو **الأكثر تغطية بالاختبارات** (14 من 23 ملف اختبار تلمسه). التغطية العالية هنا ليست علامة صحة — هي **الثمن المدفوع** لصعوبة الاستدلال على الكود بالقراءة وحدها.

### أ-٣ · المستودع يحمل ضعف وزنه الضروري 🟠 عالٍ

```
مُولَّد (app.bundle.js, styles.css, qrgen.js, …) = 781 KB
مصدر  (src/)                                     = 780 KB
```

`styles.css` و`qrgen.js` و`trello.js` وغيرها **مخزَّنة مرتين بايتًا ببايت** (جذر + `src/`). و`app.bundle.js` وحده 523 KB يتغيّر مع كل بناء — فتصبح مراجعة أي طلب دمج غير عملية: الفرق الحقيقي مدفون تحت آلاف الأسطر المُولَّدة.

**لا يُحلّ بحذف الحارس.** الحارس في CI وُضع بعد حادثة نشر حقيقية وهو محقّ. الحل نقل **التوليد** إلى وقت النشر، مع إبقاء الدرس محروسًا باختبار.

### أ-٤ · لا أدوات جودة آلية 🟠 عالٍ

لا ESLint، لا فحص أنواع (ولا حتى `// @ts-check` + JSDoc)، لا مُنسّق. بوابة الجودة الوحيدة هي `node --check`.

**حدود `node --check`:** يمسك الصياغة فقط. **لا** يمسك متغيّرًا غير معرَّف، ولا `esc()` منسيًّا قبل حقن HTML، ولا وعدًا غير مُنتظَر (`await` مفقود)، ولا متغيّرًا ميتًا. وهذه بالضبط أنواع الأخطاء التي يُنتجها نطاق عام بـ403 دوال.

### أ-٥ · البناء غير قابل لإعادة الإنتاج 🟠 عالٍ

`.github/workflows/tests.yml` يشغّل `npm install jsdom@26.1.0 --no-save`. **لا `package-lock.json` في المستودع.**

**ليس افتراضيًا:** فشلت 16 من 23 مجموعة اختبار في هذه البيئة لهذا السبب حرفيًا (`Cannot find module 'jsdom'`)، ثم صارت 468/468 خضراء بعد تثبيت الاعتمادية يدويًا. أي بيئة نظيفة لا تعرف كيف تشغّل هذا المشروع.

### أ-٦ · سجلّ Git لا يحكي قصة 🟡 متوسط

عشر دفعات متتالية برسالة **متطابقة حرفيًا**: `feat(capacity): تخطيط الطاقة بالمسمّى الوظيفي لا بالشخص`. السجل — وهو أرخص توثيق ممكن — لا يمكن الاستدلال منه على ما تغيّر ولا لماذا.

---

## القسم ب — الواجهة (UI)

### ب-١ · نظام التصميم فقد نصف سلطته 🟠 عالٍ

```bash
grep -rno 'style="' src/*.js src/app/*.js | wc -l   # 495
grep -o 'style="[^"]*"' src/index.html | wc -l       # 57
```

**552 قرارًا بصريًا مكتوبًا سطريًا** — خارج نظام التصميم. (تفكيك أدقّ لاحقًا: **454 ثابتًا** و**92 ديناميكيًا** يحمل قيمة محسوبة — والديناميكي استعمال مشروع لا يمكن التعبير عنه بصنف، فالهدف الصحيح «صفر ثابت» لا «صفر مطلق».) والنظام نفسه **ممتاز**: 756 متغيّر CSS، سلّم تباعد 4px، أربع طبقات ظلال، رموز نبرة كاملة. المشكلة ليست في النظام بل في أن نصف الواجهة **لا يمرّ به**.

**الاختبار العملي:** غيّر `--gold` إلى لون آخر. سيتغيّر نصف الواجهة، ويبقى النصف الآخر ذهبيًا. هذا تعريف نظام تصميم فقد سلطته.

**وله ثمن غير متوقع:** الأنماط السطرية تفرض `unsafe-inline` في أي CSP مستقبلية (انظر د-١). فجوة تصميمية تحوّلت إلى **قيد أمني**.

### ب-٢ · ثماني نوافذ حوارية بلا مكوّن واحد 🟠 عالٍ

> **حالة اليوم بعد W4-أ:** الـ`onclick` السطري **أُزيل بالكامل** (صفر في الهيكل) واستُبدل
> بسمة `data-close` ومعالج مفوَّض واحد — وهو شرط تقني لأي CSP بلا `unsafe-inline`.
> وترميز النوافذ صار أصنافًا مشتركة (`rq-x` · `rqbody` · `rqmodal.w-*`) بدل تكراره في
> كل نافذة. **الباقي** هو توحيد السلوك نفسه: استيعاب النوافذ السبع في `dialog()` القائم
> (موضعه W3)، فالمكوّن الصحيح موجود ولا تمرّ به.

ثماني طبقات `rqoverlay/rqmodal` كانت مكتوبة يدويًا في `src/index.html`، بعضها بـ`onclick=` سطري:

```html
<button onclick="document.getElementById('holOverlay').style.display='none'" …>
```

النتيجة: سلوك المفاتيح **غير موحّد** عبر النوافذ — الإغلاق بـEsc، حبس التركيز داخل النافذة (focus trap)، استعادة التركيز للزر الذي فتحها، `aria-modal`/`role="dialog"`. كل نافذة تنال ما ناله كاتبها من انتباه، والمستخدم بلوحة مفاتيح يدفع الفرق.

### ب-٣ · لا وضع داكن 🟡 متوسط

```bash
grep -c "prefers-color-scheme" src/styles.css   # 0
```

مع 756 متغيّرًا معرَّفًا في `:root` يبدو أن البنية جاهزة. **وهذا التقدير كان خاطئًا.**

> **تصحيح بالقياس (W4):** الرموز موجودة، لكن نصف الورقة النمطية **لا يمرّ بها**:
> **176 لونًا ست عشريًا و51 لون `rgba`** مكتوبة صراحةً خارج `:root`، منها `#fff` وحدها
> **107 مرات**. قلب الرموز وحده كان سيُنتج واجهة نصفها داكن ونصفها لوحات بيضاء —
> أي لا وضعًا داكنًا ولا وضعًا فاتحًا.
>
> والأخطر أن `#fff` كان يؤدي **دورين متعارضين** لا يمكن تمييزهما آليًا:
> **42 سطحًا** (يجب أن ينقلب) و**48 نصًّا فوق سطح ملوّن أو داكن** (يجب أن يبقى أبيض).
> ولهذا لم يكن الوضع الداكن «إعادة تعريف رموز» بل **ترميزًا للألوان أولًا**.
>
> نُفِّذ الترميز: `--paper` للأسطح و`--on-solid` للنص فوقها، و86 موضعًا انتقل إليهما،
> مع استثناءين واعيين محروسين باختبار: كتل `@media print` (الورق أبيض دائمًا) وخلفية
> رمز QR (الماسحات تشترطها). المتبقّي **91** لونًا صريحًا، محروسًا بسقف لا يرتفع.

الفجوة الأصلية باقية إذن، لكن **حجمها صار معروفًا وطريقها مفتوحًا**: الوضع الداكن يأتي بعد اكتمال الترميز، لا قبله.

### ب-٤ · الجوال يفقد أدوات لا يُعوَّض عنها 🟠 عالٍ

- **القفز السريع يختفي كليًا:** `@media(max-width:900px){.qjump-wrap{display:none!important}}` — أهم أداة تنقّل في المنصة، **بلا بديل** على الجوال. الإخفاء قرار سليم (الحقل لا يليق بشريط ضيّق)؛ **غياب البديل** هو الفجوة.
- **جدول الـ13 عمودًا** (`المعرف · الاسم · النوع · مدة · بداية · نهاية · الحالة · تقدّم · التأخير · متطلبات · المسمّى · المخرج · تحرير`) يُعالَج بـ`overflow-x:auto` فقط: لا عرض بطاقات، ولا تثبيت لعمود المعرّف — فيتيه المستخدم عن هوية الصف بمجرد التمرير أفقيًا.

---

## القسم ج — التجربة (UX)

### ج-١ · النمط الصحيح موجود ولم يُعمَّم 🟠 عالٍ

`preserveFocus` حلّ ناضج لمشكلة حقيقية (فقدان التركيز وموضع التمرير عند إعادة البناء). لكن:

```bash
grep -rn "preserveFocus(" src/*.js src/app/*.js   # موضعان فقط
grep -rno "\brender()" src/*.js src/app/*.js | wc -l   # 61 موضع استدعاء
```

**اثنان من 61.** الـ59 الباقية ما زالت تُسقط المستخدم من مكانه. هذه ليست فجوة معرفة — الحل مكتوب ومُختبَر — بل **فجوة تعميم**: النمط الصحيح متاح ولم يُجعل المسار الافتراضي.

### ج-٢ · الخطأ يختفي بعد 3.2 ثانية إلى الأبد 🟠 عالٍ

271 استدعاء `toast` هي **قناة الأخطاء الوحيدة**. والـtoast: يختفي تلقائيًا، لا يُنسخ نصه، لا يُستعاد، ولا يترك أثرًا.

**السيناريو الواقعي:** يضغط المستخدم «اعتماد العقد»، ينظر بعيدًا لحظتين، يعود فيجد الشاشة كما هي بلا تفسير. الرسالة التي كانت ستشرح كل شيء ذهبت. فيعيد المحاولة — على إجراء حوكمي.

وهذا **أشدّ** حدّةً هنا تحديدًا، لأن `DESIGN.md §7` يُلزم بأن تشرح الرسالة وتقترح — أي أن المنصة تستثمر جهدًا حقيقيًا في صياغة رسائل جيدة، ثم تعرضها في قناة تُتلفها بعد ثلاث ثوانٍ.

### ج-٣ · التراجع في مسار واحد 🟡 متوسط

`toastUndo` نمط ممتاز (8 ثوانٍ + زر تراجع) — مستخدم في مسار واحد بينما الإجراءات الهدّامة كثيرة: حذف بند، حذف متطلب، حذف تعليق، حلّ حزمة، مسح خطة، حذف مرحلة.

### ج-٤ · لا يمكن مشاركة «العرض كما تراه» 🟡 متوسط

الروابط العميقة ناضجة للمشروع — `#/c/{شريك}/{مشروع}/{تبويب}/t/{بند}` مع دعم أبدي للصيغة القديمة (عمل ممتاز). لكنها **تقف عند حدود المشروع**: فلاتر الجدول والجانت (`TFILTER`, `GSCALE`, `GCRIT`, `PKG_COLLAPSED`) وفلاتر المحفظة تعيش في متغيّرات عامة و`localStorage`، لا في العنوان.

**الأثر:** لا يستطيع مدير المكتب إرسال «كل البنود الحرجة المتأخرة عند الشريك» كرابط. يرسل بدلها تعليمات: «افتح الجدول ثم فعّل فلتر كذا…».

### ج-٥ · حالة التحميل غير موحّدة 🟢 منخفض

هياكل عظمية (skeleton) في 4 مسارات (`timeline`, `discuss`, `requests`, `audit`) — وفراغ صامت في البقية.

---

## القسم د — الأمان والأداء

### د-١ · لا CSP 🔴 حرج

`_headers` يحمل ثلاث ترويسات فقط: `X-Frame-Options`, `X-Content-Type-Options`, `Referrer-Policy`. **لا `Content-Security-Policy`، ولا `Strict-Transport-Security`، ولا `Permissions-Policy`.**

والصفحة تحقن HTML في **176 موضع `innerHTML`**، محميّة بـ`esc()` يدويًا في 390 موضعًا. **النسبة صحية والانضباط حقيقي** — لكنه دفاع من طبقة واحدة يعتمد على ألا يُنسى `esc()` **ولا مرة واحدة** عبر 176 موضعًا وكل ما سيُضاف مستقبلًا. CSP هي الطبقة الثانية الغائبة.

**تسلسل سببي مهم:** CSP بلا `unsafe-inline` **مستحيلة اليوم** بسبب 552 نمطًا سطريًا (ب-١). فجوة التصميم تحجب الإصلاح الأمني — ولهذا يجب أن يسبق إصلاحُ ب-١ إصلاحَ د-١، لا العكس.

### د-٢ · سكربت طرف ثالث بلا SRI ولا تثبيت إصدار 🟠 عالٍ

```html
<script defer src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
```

وسم **متحرّك** (`@2` = أي إصدار ثانوي قادم) من CDN خارجي، **بلا `integrity`** و**بلا `crossorigin`**. أي اختراق للـCDN — أو إصدار ثانوي مُخترَق — يعني تنفيذ كود عشوائي في سياق موثوق يملك جلسة المستخدم ومفتاح Supabase.

### د-٣ · الأداء بلا ميزانية ولا قياس 🟡 متوسط

التقسيم الكسول لست وحدات ثقيلة (`qrgen`, `importer`, `dol`, `pgantt`, `timeline`, `trello`) **قرار معماري صحيح ومطبَّق**. لكن:

- النواة **523 KB غير مصغَّرة** — لا minify ولا tree-shaking (وهو ما لا يتيحه الدمج النصي أصلًا — انظر أ-١).
- الخط من Google Fonts عبر `<link rel="stylesheet">` **حاجب للتصيير** (وإن خفّف `display=swap` أثره).
- **لا ميزانية أداء ولا قياس في CI** — لا أحد يعرف متى يتضخّم الحجم، فيتضخّم.

---

## القسم هـ — الاختبار

### هـ-١ · التغطية لا توازي المخاطر 🟠 عالٍ

| الوحدة | ملفات اختبار تستهدفها |
|---|---|
| `contractshub.js` | **14** |
| `api.js` | 10 |
| `views.js` | 6 |
| `portfolio.js` · `contractsign.js` | 4 |
| **`engine.js` (محرك CPM)** | **1** |
| `taskpanel` · `state` · `staffaccess` · `lifecycle` · `dialogs` · `dol` · `importer` · `pgantt` · `timeline` · `trello` | **0** |

> **تصحيح قياس:** أوردت نسخة سابقة من هذا المستند «5 ملفات بلا اختبار». العدد الصحيح
> **10**. الخطأ منهجي لا حسابي: الأمر الأول بحث في `tests/` كاملًا — فطابَق أسماء الوحدات
> الكسولة داخل `tests/run.js` و`tests/README.md` وحسبها «مغطّاة». القياس الصحيح يقصر
> البحث على ملفات `test_*.js` وحدها. وهذا بالضبط ما تحذّر منه الملاحظة المنهجية الختامية
> في `DESIGN.md`: **القياس الخام بلا تفكيك يُنتج استنتاجًا خاطئًا** — ولهذا صار العدد
> اليوم محروسًا آليًا في `tests/run.js` بدل أن يُقاس يدويًا عند الحاجة.

المفارقة صارخة: **أخطر كود في المنصة هو الأقل تغطية**. `scheduleTasks` تنتج كل تاريخ يراه المستخدم، وكل حكم بالتأخير، وكل حساب طاقة — وفيها منطق دقيق يستحق مصفوفة حالات كاملة: روابط `FS`/`SS`/`FF`، إزاحة سالبة (`lag < 0`)، مدد صفرية، بنود مستمرة (`cont`)، حزم عمل، والمرور العكسي (`LS`/`LF`) الذي يتضمن **عكسًا يدويًا دقيقًا** لمنطق المرور الأمامي (`invLag` + شرط `prevWD`) — وهو أدقّ موضع في الملف وأقربه للخطأ الصامت.

الخطأ هنا **لا يُسقط الصفحة** عادةً؛ يُنتج تاريخًا خاطئًا بثقة، فيُتّخذ قرار حوكمي على أساسه.

> **ما وجدته مصفوفة الحالات فور كتابتها (W7-1):** الافتراض أعلاه كان **أفضل من الواقع**. حالة واحدة كانت تُسقط الصفحة فعلًا: أي **تبعية دائرية** تجعل `scheduleTasks` ترمي `TypeError` — لأن المرور الخلفي يقرأ `LS`/`LF` للاحق لم يُحسب بعد حين يسقط الترتيب الطبولوجي. وبما أن `compute()` تُستدعى بلا حماية داخل `render()`، تبقى **شاشة المشروع بيضاء كليًا بلا مخرج من الواجهة**. والحالة قابلة للبلوغ: المستورد يكتشف الدورة ويحذّر منها **ولا يمنعها**، و`bulkInsertDeps` لا تتحقق منها إطلاقًا. والمحرك نفسه كان يُعلن نيّة التعامل معها بلطف (`hasCycle` + تحذير «الجدولة غير دقيقة») — النيّة كانت موجودة والتنفيذ ناقصًا. أُصلح، ومحروس الآن بستة تأكيدات.

### هـ-٢ · «468 اختبارًا» رقم بلا مقام — والمقام غير قابل للقياس أصلًا 🟠 عالٍ

لا قياس تغطية. العدد المطلق لا يخبر بما هو **غير** مغطّى — وقد رأينا للتو أن ما ليس مغطّى هو الأخطر.

**والأعمق من ذلك:** جُرِّب `c8` فعليًا على هذه المجموعة فأعاد **`0/0` — لا شيء**. السبب بنيوي لا إعدادي:

- الاختبارات التصييرية تحقن `app.bundle.js` **كنص** داخل نافذة jsdom (`script.textContent = …`) — فلا يرى V8 ملفًا على القرص لينسب إليه التغطية.
- الاختبارات النصية تقرأ المصدر **كنص ولا تنفّذه** أصلًا.

أي أن قياس التغطية الحقيقي **محجوب بالموجة W2**: لا يصير ممكنًا إلا حين تُحمَّل الوحدات كوحدات. وهذه **تبعية سببية ثالثة** لم تظهر في التحليل الأول، وتنضم إلى تبعيتَي «الأنماط السطرية تحجب CSP» و«النطاق العام يحجب التصغير».

### هـ-٣ · لا حراسة آلية للغة التصميم ولا لإتاحة الوصول 🟡 متوسط

`DESIGN.md` وثيقة استثنائية الجودة، و`test_design_language.js` خطوة صحيحة نحو حراستها. لكن لا فاحص a11y آلي (axe) ولا انحدار بصري — فتُحرَس القواعد نصيًا لا سلوكيًا.

---

## خلاصة التشخيص

| # | الفجوة | الحدّة | يحجب |
|---|---|---|---|
| أ-١ | لا نظام وحدات · 403 دالة عامة | 🔴 | التصغير · tree-shaking · أمان إعادة التسمية |
| أ-٢ | دالة بـ820 سطرًا | 🔴 | اختبار الوحدات · سرعة التعديل الآمن |
| د-١ | لا CSP | 🔴 | الطبقة الدفاعية الثانية |
| أ-٣ | 781 KB مُولَّد داخل المستودع | 🟠 | مراجعة الكود |
| أ-٤ | لا فحص آلي (lint/types) | 🟠 | ضبط النطاق العام |
| أ-٥ | لا قفل اعتماديات | 🟠 | إعادة إنتاج البناء |
| ب-١ | 552 نمطًا سطريًا | 🟠 | الوضع الداكن · CSP الصارمة |
| ب-٢ | 8 نوافذ بلا مكوّن موحّد | 🟠 | إتاحة الوصول بلوحة المفاتيح |
| ب-٤ | الجوال بلا بديل للتنقّل | 🟠 | استخدام ميداني |
| ج-١ | `preserveFocus` في 2 من 61 | 🟠 | استقرار التحرير |
| ج-٢ | الخطأ يتلاشى بلا أثر | 🟠 | الثقة والتعافي |
| د-٢ | CDN بلا SRI ولا تثبيت | 🟠 | سلسلة التوريد |
| هـ-١ | محرك CPM بتغطية ملف واحد · **10** ملفات بلا اختبار | 🟠 | صحة كل تاريخ معروض |

---

## قرار معماري: لا لإعادة الكتابة بإطار

الاستنتاج الأسهل من تحليل كهذا هو «أعيدوا بناءها بـReact». **هذا هو القرار الخاطئ هنا**، لثلاثة أسباب قابلة للقياس:

1. **468 اختبارًا مبنيّة على DOM مباشر.** إعادة الكتابة تُسقط شبكة الأمان كلها **دفعة واحدة** — في اللحظة نفسها التي تكون فيها أشدّ حاجة إليها.
2. **السلوك الناضج غير موثّق بالكامل في الكود.** ثلاثة مسارات لاستعادة الطباعة، توافق أبدي مع الروابط القديمة، عكس دقيق لـCPM في المرور الخلفي — هذه معرفة اشتُريت بحوادث. إعادة الكتابة تعيد شراءها بحوادث جديدة.
3. **كل فجوة أعلاه قابلة للإصلاح تدريجيًا** والاختبارات خضراء بعد كل خطوة. لا فجوة واحدة تستلزم تغيير النموذج البرمجي.

وهذا تحديدًا ما تحذّر منه الملاحظة المنهجية الختامية في `DESIGN.md`: **تغيير منطق مستقر بلا مبرر مخاطرة لا إنجاز.**

المسار المقترح — بموجات محروسة بالاختبارات، كل موجة قابلة للنشر بذاتها — في «المسار الهندسي» ضمن [`ROADMAP.md`](ROADMAP.md).
