# منصّة إدارة المشاريع — علامة (Alaamaa-PMO)

داشبورد حوكمة المشاريع بين مكتب إدارة المشاريع (PMO) في علامة وعملائها.

- **النشر:** Cloudflare Pages على `pmo.alaamaa.com` (تلقائي عند الدفع لـ`main`)
- **القاعدة والمصادقة:** Supabase (مشترك مع الأدمن، جداول `pmo_` معزولة)
- **الدخول:** Google (حساب علامة)

## الأدوار والصلاحيات
- **PMO (admin):** بناء وتعديل الخطة، اعتماد Baseline، بتّ طلبات التغيير، إدارة وصول العملاء، تحويل العملاء المحتملين.
- **فريق التسويق (manager):** تحديث الحالة والتقدّم والمتطلبات، رفع طلبات تغيير.
- **العميل:** عرض مشروعه فقط (لوحة قيادة، جانت، مخرجات)، رفع طلبات، النقاش. (عزل بـRLS)

## البنية المعمارية (مهم للتطوير)

التطوير يتم في `src/` حصرًا. و`build.py` يدمجها في حزمة النواة والوحدات الكسولة وقت **النشر**. **لا تحرّر الملفات الجذرية المُولَّدة** — فهي ناتج، وليست في المستودع أصلًا.

```
src/
├── index.html   ← الهيكل (HTML) فقط — يحمّل الوحدات بالترتيب
├── styles.css   ← نظام التصميم (متغيّرات، ظلال، حالات، استجابة)
├── config.js    ← الإعدادات والثوابت (Supabase، الألوان، الأدوار، الصلاحيات PERMS)
├── engine.js    ← محرك CPM الخالص (scheduleTasks + computeTracking) — بلا تبعيات
├── api.js       ← طبقة البيانات: كل وصول لـSupabase معزول هنا حصرًا
├── views.js     ← دوال العرض (render + v*) وربط أحداثها (bind*)
└── app.js       ← الحالة، النوافذ (toast/dialog)، الشاشات، المعالجات
```

### مبدأ معماري: عزل طبقة البيانات
**كل** استدعاء لـSupabase (`sb.from`, `sb.rpc`, `sb.auth`) موجود في `api.js` فقط. بقية الملفات تستدعي دوال `api` (مثل `fetchPortfolio`, `updateTaskFields`, `insertCR`). عند تغيير مخطّط جدول، التعديل في مكان واحد.

### ترتيب تحميل الوحدات
`config` → `engine` → `api` → `views` → `app` (التبعيات من الأعلى للأسفل).

## سير العمل
1. عدّل ملفات `src/` — **هي وحدها المُتتبَّعة**.
2. شغّل `npm run check` محليًا (يبني ويفحص ويختبر).
3. ادفع لـ`main` → **Cloudflare يبني بنفسه** بالأمر `python3 build.py` وينشر.

> **الملفات المُولَّدة لم تعد في المستودع** (منذ الموجة W1): `app.bundle.js` ·
> `styles.css` · `index.html` · والوحدات الكسولة الست. تُبنى وقت النشر، وتوجد محليًا
> بعد `npm run build` لكنها في `.gitignore`. **لا تلتزمها** — الالتزام يخلق مصدر
> حقيقة ثانيًا يتعارض مع ما يبنيه النشر، وCI يرفضه.

## أوامر التطوير

```bash
npm ci             # تثبيت الاعتماديات من ملف القفل (لا npm install)
npm run build      # توليد index.html و app.bundle.js و styles.css من src/
npm run lint       # ESLint — متغيّر غير معرَّف · كود ميت · تعريف مزدوج (سقف تحذيرات ثابت)
npm run typecheck  # فحص أنواع محرك CPM (نطاقه مقيَّد بقصد — التبرير في jsconfig.json)
npm test           # الاختبارات + حرّاس النشر والاتّساع ونظام التصميم
npm run check      # الأربعة معًا — نفس ما يشغّله CI بالضبط
```

**مرة واحدة على جهازك:** `git config commit.template .gitmessage`
(CI يرفض رسالة دفعة مكرَّرة حرفيًا عن سابقتها — راجع `AUDIT.md` §أ-٦.)

## التحقق قبل الدفع
- توازن الأقواس + `node --check` على JS المُولّد.
- محاكاة تحميل الوحدات (vm) للتأكد من الترابط.
- اختبار تغييرات القاعدة ضمن معاملة متراجَع عنها.

## المراجع
- [`ROADMAP.md`](ROADMAP.md) — مسار المنتج (المراحل) **والمسار الهندسي** (الموجات W0…W7).
- [`AUDIT.md`](AUDIT.md) — التحليل الهندسي: الفجوات بأدلّتها وقياساتها وحدّتها.
- [`DESIGN.md`](DESIGN.md) — لغة التصميم: كل نمط مطبَّق ومُختبَر، مع سبب نشأته.
